<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.illinois.cs465.ddc">

    <uses-sdk android:minSdkVersion="21" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/TitleTheme">
        <activity
            android:name=".StartActivity"
            android:theme="@style/TitleTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RaceSelectActivity" />
        <activity android:name=".CharBackgroundActivity" />
        <activity android:name=".ClassSelectActivity" />
        <activity android:name=".CurrencyActivity" />
        <activity android:name=".InventoryActivity" />
        <activity android:name=".EquipmentActivity" />
        <activity android:name=".CharacterMainScreenActivity" />
        <activity android:name=".StatsEditActivity" />
        <activity android:name=".AbilityEditActivity"></activity>
        <activity android:name=".NoteActivity" />
        <activity android:name=".SuppliesActivity"/>
        <activity android:name=".NoteListActivity" />
        <activity android:name=".CombatTabActivity" />
    </application>

</manifest>